# TASK-002: 튜브 제외 캐릭터들 프렌즈 정보 크기 키우기

## 메타데이터

| 항목 | 값 |
|------|-----|
| Task ID | TASK-002 |
| 우선순위 | Medium |
| 상태 | 🔴 Blocked (추가 정보 필요) |
| 예상 영향 범위 | 영웅 UI |
| 관련 이슈 | PENDING-ISSUES.md #2 |

---

## 1. 개요

### 1.1 문제 정의
튜브 캐릭터를 제외한 나머지 캐릭터들의 "프렌즈 정보" UI에서 캐릭터 표시 크기가 작습니다.

### 1.2 목표
- 튜브를 제외한 모든 캐릭터의 UI 표시 크기 증가
- 일관된 시각적 경험 제공
- 튜브 캐릭터의 현재 크기 유지

---

## 2. 현황 분석

### 2.1 관련 파일

| 파일 | 역할 |
|------|------|
| `assets/Game/Script/ui/heroItem.ts` | 영웅 아이템 UI 컴포넌트 |
| `assets/resources/prefabs/UIHeroShop.prefab` | 영웅 상점 프리팹 |
| `assets/resources/heros/hero00X/` | 개별 영웅 프리팹 |

### 2.2 추정 코드 위치

```typescript
// heroItem.ts - 영웅 아이템 설정 부분
// 스켈레톤 또는 스프라이트 스케일 설정이 있을 것으로 예상
```

---

## 3. 요구사항 (Pending)

### 3.1 필수 입력 정보

| 항목 | 상태 | 설명 |
|------|------|------|
| 튜브 캐릭터 ID | ❌ 미제공 | 튜브가 어떤 영웅 ID인지 |
| 크기 증가 비율 | ❌ 미제공 | 얼마나 키울지 (예: 1.2배, 1.5배) |
| 적용 위치 | ❓ 미확인 | 상점만? 또는 다른 UI도? |

### 3.2 참고 질문

```
1. "튜브"는 어떤 영웅 ID에 해당하나요? (1~8 중)
2. 나머지 캐릭터들을 현재 대비 몇 배로 키워야 하나요?
   - 예: 1.2배 (20% 증가)
   - 예: 1.5배 (50% 증가)
3. 영웅 상점 외에 다른 UI에도 적용해야 하나요?
```

---

## 4. 수정 계획 (정보 제공 후)

### 4.1 방법 A: 스크립트 수정 (권장)

```typescript
// heroItem.ts
const TUBE_HERO_ID = ???;  // 튜브 영웅 ID
const SCALE_MULTIPLIER = ???;  // 예: 1.2

// 영웅 설정 시
setHero(heroId: number) {
    // ... 기존 코드 ...
    
    // 튜브가 아닌 캐릭터 스케일 조정
    if (heroId !== TUBE_HERO_ID) {
        this.heroSprite.node.scale *= SCALE_MULTIPLIER;
        // 또는
        // this.skeletonNode.scale *= SCALE_MULTIPLIER;
    }
}
```

### 4.2 방법 B: 프리팹 직접 수정

각 영웅 프리팹에서 스케일 값 직접 수정:

```json
// hero00X.prefab (튜브 제외)
"_scale": {
    "__type__": "cc.Vec3",
    "x": 1.2,  // 증가된 스케일
    "y": 1.2,
    "z": 1
}
```

### 4.3 방법 C: 개별 영웅별 스케일 테이블

```typescript
// 영웅별 커스텀 스케일
const HERO_SCALES = {
    1: 1.2,  // 정령 사수
    2: 1.3,  // 전사
    3: 1.2,  // 화총수
    4: 1.1,  // 법사
    5: 1.0,  // 튜브 (유지)
    6: 1.2,  // 수전사
    7: 1.2,  // 열화룡
    8: 1.2,  // 악마녀
};
```

---

## 5. 검증 계획

### 5.1 테스트 케이스

| 테스트 | 기대 결과 |
|--------|----------|
| 튜브 캐릭터 표시 | 기존 크기 유지 |
| 나머지 캐릭터 표시 | 지정된 배율로 증가 |
| 영웅 상점 레이아웃 | 크기 증가 후 UI 겹침 없음 |
| 다른 UI 화면 | 영향 없음 (또는 동일 적용) |

### 5.2 확인 포인트

- [ ] 캐릭터가 UI 영역을 벗어나지 않는지
- [ ] 다른 UI 요소와 겹치지 않는지
- [ ] 텍스트 레이블이 가려지지 않는지

---

## 6. 체크리스트

### 정보 제공 체크리스트
- [ ] 튜브 캐릭터 ID 확인
- [ ] 원하는 크기 배율 확인
- [ ] 적용 범위 확인 (상점만 / 전체 UI)

### 구현 체크리스트 (정보 제공 후)
- [ ] heroItem.ts 분석
- [ ] 스케일 조정 코드 추가
- [ ] 튜브 제외 로직 구현
- [ ] 영웅 상점 테스트
- [ ] 다른 UI 영향 확인

---

## 7. 담당자 액션

**정보 제공 요청:**
```
1. "튜브" 캐릭터의 영웅 ID는 몇 번인가요?
2. 나머지 캐릭터들을 몇 배로 키워야 하나요? (예: 1.2배)
3. 영웅 상점 외에 다른 화면에도 적용해야 하나요?
```
